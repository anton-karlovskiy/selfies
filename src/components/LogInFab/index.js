
import React from 'react';

import FloatingActionButton from 'components/UI/FloatingActionButton';
import './log-in-fab.css';

const LogInFab = props => (
  <FloatingActionButton
    {...props}
    className='log-in-fab' />
);

export default LogInFab;
